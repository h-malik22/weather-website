<!DOCTYPE html>

<html>
    <head>
        <title>404</title>
        <link rel="stylesheet" href="/css/style.css">
        {{>icon}}
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMsg}}</p>
        </div>

        {{>footer}}
    </body>
</html>